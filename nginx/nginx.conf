upstream auditor_backend {
    server auditor:8000;
}

server {
    listen 80;
    server_name _;

    # 上傳檔案大小限制
    client_max_body_size 10M;

    # Gzip 壓縮
    gzip on;
    gzip_types text/plain text/css application/json text/markdown;
    gzip_min_length 256;

    # 安全 Headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' cdn.jsdelivr.net; style-src 'self' 'unsafe-inline' fonts.googleapis.com cdnjs.cloudflare.com; font-src fonts.gstatic.com;" always;

    # 反向代理至 FastAPI
    location / {
        proxy_pass http://auditor_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket 支援 (如未來需要)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # 超時設定 (稽核與 LLM 翻譯可能需要較長時間)
        proxy_read_timeout 600s;
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        send_timeout 600s;
    }
}
